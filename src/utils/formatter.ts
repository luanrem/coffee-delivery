export const BRFormatter = new Intl.NumberFormat('pt-BR', {
  style: 'currency',
  currency: 'BRL',
})

export const USFormatter = new Intl.NumberFormat('en-US', {
  style: 'currency',
  currency: 'USD',
})
